# House convention: UTF-8, LF endings, on a Windows host with core.autocrlf likely true.
# Without this, checkout rewrites every text file to CRLF and the next diff is noise.
* text=auto eol=lf

# Never touch these.
*.png binary
*.jpg binary
*.ico binary
*.pdf binary
