{
  "name": "skynet-hr",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted browser console that drives a coding agent CLI",
  "engines": { "node": ">=22.11.0" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/**/*.test.js\""
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^20.14.0"
  }
}
